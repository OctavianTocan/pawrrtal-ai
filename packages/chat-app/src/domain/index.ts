/** Barrel for the domain layer (schemas + typed errors). */
export { ConversationNotFound, ModelNotFound } from './errors';
export {
  Conversation,
  HomeMode,
  Message,
  MessageRole,
  Model,
  ModelTier,
} from './models';
