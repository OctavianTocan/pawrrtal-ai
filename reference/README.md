# Pattern reference index

Compressed rules reused across lessons. **Read these before writing code** — lessons link here instead of duplicating prose.

| Doc | Topic | Use when |
|-----|-------|----------|
| [config.md](./config.md) | Effect `Config` for env vars | Any feature-owned env read in `apps/api` |

Populate the architecture reference vendor before grepping layout examples:

```bash
./scripts/setup-vendor-effect-api-layout.sh
```

Paths below use `backend/vendor/effect-api-layout/` (gitignored local clone).
