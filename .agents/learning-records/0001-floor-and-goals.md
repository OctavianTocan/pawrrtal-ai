# Floor: low prior Effect knowledge, strong learning intent

User self-reports having "sort-of" used `Context.Service` and `Layer` and
`HttpApiBuilder`/`HttpMiddleware`, but does not really understand them.
Implication: Lesson 1 must be foundation, not auth. Auth is decoration on
top of the service / layer kernel; teaching auth first would force me to
filler-introduce services mid-lesson, which is exactly the failure mode the
teach skill warns against.
