# Lessons (auto-distilled + manually curated)

> Entries here outlive specific tasks. The dream cycle promotes recurring
> patterns from episodic into this file. Feel free to curate manually —
> delete bad lessons, tighten wording, reorganize sections.

## Seed lessons
- Always read `protocols/permissions.md` before any destructive tool call.
- Write the failing test before writing the fix.
- Log to episodic memory on every significant action, success or failure.
- When a skill has failed 3+ times in 14 days, propose a rewrite.
- Never force push to protected branches under any circumstance.

## Auto-promoted entries will be appended below

### 2026-06

- For Pawrrtal SpecKit work, verify the active feature directory and the user's latest explicit target before planning, tasks, or implementation; ask before changing feature scope or choosing a different spec number.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_49da2c81d8a3 -->
- Treat AGY or other external model reviews of private or uncommitted repo code as external code sharing; get explicit approval after naming the risk, otherwise do a local review and report the blocker.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_54b96dcd408d -->
- For Bun Effect CLI work, load code-quality, domain-cli, and domain-effect/effect-smol before coding; Bun runtime, Effect v4 idioms, Schema boundaries, and contract docstrings are first-pass implementation constraints.  <!-- status=accepted confidence=0.6 evidence=1 id=lesson_4dc9bc7c45ea -->

### 2026-04

- Always serialize timestamps in UTC to avoid cross-region comparison bugs  <!-- status=accepted confidence=0.46 evidence=1 id=lesson_422695ae5b2d -->
